#include <stdio.h>
#include "max.h"
#include "avg.h"
int main(void) {
int i = 1700000, j = -20;
	printf("Max of number is : %d \n", MAXn);
	printf("Max between i and j is : %d \n", max(i, j));
	printf("Average of i and j is : %f \n", average(i, j));
return 0;
}

